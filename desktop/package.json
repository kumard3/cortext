{
  "name": "tribe-scorer-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell for TRIBE Scorer (macOS, Apple Silicon)",
  "scripts": {
    "sync": "bash scripts/sync-resources.sh",
    "tauri": "tauri",
    "dev": "bash scripts/sync-resources.sh && tauri dev",
    "build": "bash scripts/sync-resources.sh && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
